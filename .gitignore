node_modules/
# Anchored: the server's SQLite dir only. An unanchored "data/" also swallowed
# android/app/src/main/java/com/marusic/app/data/, which broke the CI build.
/data/
.env
.claude/settings.local.json
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Android app (android/ has its own .gitignore too)
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/.kotlin/
android/keystore/
android/keystore.properties
